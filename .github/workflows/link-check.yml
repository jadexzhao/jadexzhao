name: Link Check

on:
  push:
    branches: ["main"]
  pull_request:
  schedule:
    - cron: "0 9 * * 1"

permissions:
  contents: read

jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check README links
        uses: lycheeverse/lychee-action@v2
        with:
          # Exclude LinkedIn (often blocks automated checks) and allow failures to avoid blocking deploys
          args: --no-progress --verbose README.md --exclude "https://www.linkedin.com/*" 
          fail: false
